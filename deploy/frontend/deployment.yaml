apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: rps
  labels:
    app: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: frontend
          image: ghcr.io/dgkim-lab/kube-hello-kiali-frontend:4df333819e4b9cb574421cba00d1ae19528d8c82
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
